package com.btsl.pretups.filters;

import jakarta.servlet.ServletConfig;
import jakarta.servlet.ServletException;

import com.btsl.logging.Log;
import com.btsl.logging.LogFactory;
//import com.inet.config.ConfigurationManager;

public class CReportServletJSP /*extends com.inet.http.PluginDispatcherServlet*/ {

/*
	public void init(ServletConfig config) throws ServletException {

		try {
			ConfigurationManager.getInstance().getCurrent().put("PropertyChecker",
					"com.inet.customplugin.InetPropertiesChecker");
			//super.init(config);
		} catch (Exception e) {
			System.out.println("222 Init Exception " + e);
			e.printStackTrace();
		}

	}
*/

}
